{
  "name": "speed-bac-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Speed Bac Mobile — Real-time multiplayer word game PWA",
  "scripts": {
    "postinstall": "npm install --include=dev --prefix client && npm install --include=dev --prefix server",
    "dev": "npx concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "node server/src/index.js",
    "dev:client": "npm run dev --prefix client -- --host",
    "build": "npm run build --prefix client",
    "start": "NODE_ENV=production node server/src/index.js"
  },
  "dependencies": {
    "concurrently": "^10.0.3"
  }
}
